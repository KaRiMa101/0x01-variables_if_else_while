#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - this is main program
 * Description:  print numbers 0 to 9
 * followed by a new line
 * Return: Always 0
 */
int main(void)
{
int l;
for (l = 0; l < 10 ; l++)
{
printf("%d", l);
}
printf('\n');
return (0);
}
